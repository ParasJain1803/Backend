<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Instagram</title>
    <style>
      img {
        width: 200px;
        height: 200px;
        object-fit: cover;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h2>This page belongs to @<%= data.name%></h2>
    <button>Follow</button>&nbsp;&nbsp;&nbsp;&nbsp; <button>Message</button>

    <p>Followers: <%= data.followers %> &nbsp;&nbsp;&nbsp;&nbsp Following: <%= data.following %></p>
    
    <% for (let post of data.posts) { %>
    <img src="<%= post.image %>" alt="some img" />
    <p>Likes: <%= post.likes %> &nbsp;&nbsp;&nbsp;&nbsp; Comments: <%= post.comments %></p>
    <%} %>

  </body>
</html>
